[project]
name = "jrisk-backend"
version = "0.1.0"
description = "Jrisk Backend"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "uvicorn==0.32.0",
    "fastapi",
    "firebase-admin",
    "python-dotenv",
]
