"""
Database module — SQLAlchemy engine, session factory, and ORM models.

Usage:
    from database.connection import get_session
    from database.models import Asset, RiskScore
    from database import queries
"""
